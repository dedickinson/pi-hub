---
all:
  vars:
    ansible_user: ansible
    ansible_become: yes
    ansible_ssh_private_key_file: ~/.ssh/ansible
    ansible_ssh_public_key_file: ~/.ssh/ansible.pub
    ansible_python_interpreter: /usr/bin/python3
    
  hosts:
    hub:
      ansible_host: hub.lan
